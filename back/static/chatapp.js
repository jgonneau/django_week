$("document").ready(function() {
  // Initialisation wow
  new WOW().init();
  // Prochaine implementation de socket.io
  // pour communiquer avec le Back ..
  /*
    var io = require('socket.io')(80);
  
   .. io.emit('tweet', tweet);
  */
});
